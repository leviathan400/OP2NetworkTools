; OP2Session configuration
;
; verbose = 0  -> clean output: only the high-level milestones print
;                 (discover, join, lobby, chat, start, in-game).
; verbose = 1  -> dump every packet (hex + decoded header). Debug only; very spammy.
[log]
verbose=0
